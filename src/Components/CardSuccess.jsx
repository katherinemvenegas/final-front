const CardSuccess = ({ name }) => {
    return (
        <h1 className="modal-title fs-5" id="exampleModalLabel">Gracias {name}, te contactaremos cuando antes vía mail</h1>
    );
};

export default CardSuccess;